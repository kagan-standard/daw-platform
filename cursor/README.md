# DAW Platform — Cursor Execution Pack

This folder contains prompts + runbooks + checklists so Cursor can execute safely in phases.

Key rule (server deploy):
Always target the DAW compose file explicitly:
/opt/daw-platform/infra/compose/docker-compose.yml

Use one file from /cursor/prompts at a time.
